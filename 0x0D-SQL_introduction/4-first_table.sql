-- Create the table only if it does not already exist
CREATE TABLE IF NOT EXISTS first_table (
    id INT,          -- Define id column as an integer
    name VARCHAR(256) -- Define name column as a variable-length string with a maximum length of 256 characters
);
